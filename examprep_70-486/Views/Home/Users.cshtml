@model List<examprep_70_486.Models.User>

@{
    ViewBag.Title = "Users";
}

<h2>Users</h2>

@Html.DisplayForModel()
@foreach (var item in Model)
{
    @Html.DisplayFor(m => item);
}

<h2>Add a User</h2>


@if(Model.Count > 0) {
    @Html.EditorFor(m => m[0])
}
